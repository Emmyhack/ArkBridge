# Configuration

Per-environment operational configuration: limits, roles, agent settings, monitoring
thresholds.

This is distinct from the registries in `packages/*-registry`, which define structure — which
chains exist, which assets are canonical where, which routes are permitted. Configuration here
covers operational parameters that change without changing the topology.

Production monetary limits are set here from reviewed values. They are never derived from test
configuration.
