{
  "name": "@arkbridge/bridge-core",
  "version": "0.1.0",
  "private": true,
  "description": "Framework-free bridge logic: route selection, validation, quoting, status mapping, error normalisation.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist .turbo",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@arkbridge/chain-registry": "workspace:*",
    "@arkbridge/token-registry": "workspace:*",
    "@arkbridge/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
