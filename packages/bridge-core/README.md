# @arkbridge/bridge-core

Framework-free bridge logic: route selection, validation, quote composition, fee calculation,
status mapping, message parsing, amount handling, limit handling, error normalisation.

No React. No Next.js. The SDK, the indexer, and operations scripts all depend on this, so it
must stay usable outside a browser.

Not yet implemented — Phase 7.
